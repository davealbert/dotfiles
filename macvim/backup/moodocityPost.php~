<?php

	 $db_host = 'localhost';
	 $db_name = 'moodPost';
	 $db_usr = 'mood';
	 $db_pass = 'M00dPass123';
	 $db = mysql_connect($db_host, $db_usr, $db_pass)
	 or die('Could not connect: ' . mysql_error());

	 mysql_select_db($db_name, $db);

	 if ($_GET['isAchievement'] == 1){
			$isAch = 1;
	 } else {
			$isAch = 0;
	 }

	 $APP = mysql_real_escape_string($_GET['app']);
	 $ID = mysql_real_escape_string($_GET['DeviceID']);
	 $face= mysql_real_escape_string($_GET['face']);
	 $mood= mysql_real_escape_string($_GET['mood']);
	 $SQ = mysql_real_escape_string($_GET['share']);
	 $journal = mysql_real_escape_string($_GET['journal']);
	 $datetime = mysql_real_escape_string($_GET['datetime']);
	 $lat = mysql_real_escape_string($_GET['latitude']);
	 $long = mysql_real_escape_string($_GET['longitude']);


	 $sql = "insert into posts (
			deviceid,
			face,
			mood,
			shareQuote,
			journalEntry,
			datetime,
			latitude,
			longitude,
			isAchievement) 
		
			values (
				 '$ID',
				 '$face',
				 '$mood',
				 '$SQ',
				 '$journal',
				 '$datetime',
				 '$lat',
				 '$long',
				 $isAch
			);";

	 mysql_query($sql);

	 echo '+';
?>
